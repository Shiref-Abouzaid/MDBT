<template>
    <div class="store-slider-vendor" v-if="vendors && vendors.length > 0">
        <div class="swiper arrow-secondary" v-swiper:mySwiper="swiperOption">
            <div class="swiper-wrapper">
                <div class="swiper-slide" v-for="vendor in vendors" :key="vendor.id">
                    <app-single-vendor :vendor="vendor"></app-single-vendor>
                </div>
            </div>
            <div class="swiper-button-prev" slot="button-prev">
                <img style="transform: rotate(180deg)" src="/images/icons/svg/offers-arrow.svg" alt="">
            </div>
            <div class="swiper-button-next" slot="button-next">
                <img src="/images/icons/svg/offers-arrow.svg" alt="">
            </div>
        </div>
    </div>
</template>

<script>
import SingleVednor from '~/components/Vendor/StoreVendor'

export default {
    props: ["vendors"],
    components: {
        appSingleVendor: SingleVednor
    },
    data() {
        return {
            swiperOption: {
                    slidesPerView: 'auto',
                    spaceBetween: 30,
                    // initialSlide: 1
                    // centeredSlides: true,
                    // loop: true,
                    // loopedSlides: 54,
                    navigation: {
                    nextEl: ".swiper-button-next",
                    prevEl: ".swiper-button-prev"
                    }
                    // pagination: {
                    //   el: ".swiper-pagination",
                    //   clickable: true
                    // }
            }
        }
    }
}
</script>